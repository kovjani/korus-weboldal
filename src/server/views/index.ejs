<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="/js/bundle.js"></script>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <% if (typeof page !== 'undefined') { %>
        <% if (page == 1) { %>
            <%- include("events.ejs") -%>
        <% } else if (page == 2) { %>
            <%- include("events_form.ejs") -%>
        <% } else { %>
            <%- include("home.ejs") -%>
        <% } %>
    <% } else { %>
        <%- include("home.ejs") -%>
    <% } %>
</body>
</html>